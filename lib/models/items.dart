class Item {
  final String itemname;
  final int amount;
  final String description;
  final int price;
  static final List<Item> listitems = [];

  Item(this.itemname, this.amount, this.description, this.price);
}
